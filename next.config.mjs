/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  pageExtensions: ["ts", "tsx", "mdx"],
  // If you later add image domains or rewrite proxies, add them here.
};

export default nextConfig;
